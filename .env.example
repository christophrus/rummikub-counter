# Domain für Caddy (HTTPS wird automatisch aktiviert)
DOMAIN=rummikub.example.com

# CORS Origins (optional, kommasepariert)
# In Produktion nicht nötig da alles über Caddy/nginx läuft
# CORS_ORIGINS=https://rummikub.example.com
